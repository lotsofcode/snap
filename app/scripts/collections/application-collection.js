snap.Collections.ApplicationCollection = Backbone.Collection.extend({

  model: snap.Models.ApplicationModel

});

snap.Collections.CardCollection = Backbone.Collection.extend({

  model: snap.Models.CardModel

});
